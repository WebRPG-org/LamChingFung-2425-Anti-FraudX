# Gemini API ?��??��?

## ?? 概述

?�系統現已支??**Google Gemini API**，可以使??4 ??fine-tuned Gemini 2.0 Flash 模�??�代 Ollama ?�地模�??�用?�可以在主�??�右上�?一?��???Ollama ??Gemini??

---

## ?�� 主�??�能

### ??已實?��???

1. **?�模式支??*
   - Ollama ?�地模�?（�?認�?
   - Google Gemini API（可?��?

2. **一?��???*
   - 主�??�右上�??��??��?
   - ?��??��??��?
   - 實�??��?

3. **4 ?�獨立模??*
   - 騙�? Agent (Scammer)
   - ?�害??Agent (Victim)
   - 專家 Agent (Expert)
   - 記�???Agent (Recorder)

4. **完整?�置管�?**
   - API Key 安全存儲
   - Fine-tuned 模�? ID ?�置
   - ?��??�數?��?�?

5. **?�能??��**
   - Token 使用統�?
   - ?��??��?追蹤
   - ?�本估�?
   - ?�誤?�監??

---

## ?? 快速�?�?

### 1. 安�?依賴

```bash
cd backend
pip install -r requirements.txt
```

?��?依賴�?
- `google-generativeai>=0.3.0`
- `google-ai-generativelanguage>=0.4.0`

### 2. ?��? Gemini API Key

1. 訪�? [Google AI Studio](https://aistudio.google.com/app/apikey)
2. ?��? Google 帳�?
3. 點�??�Create API Key??
4. 複製?��???API Key

### 3. ?�置?��?變�?

?�建?�編�?`.env` ?�件�?

```bash
# ?�用 Gemini（�?認為 false�?
GEMINI_ENABLED=false

# Gemini API Key
GEMINI_API_KEY=your_api_key_here

# Fine-tuned 模�? ID（可?��?默�?使用 gemini-3-flash-preview�?
GEMINI_MODEL_SCAMMER=gemini-3-flash-preview
GEMINI_MODEL_VICTIM=gemini-3-flash-preview
GEMINI_MODEL_EXPERT=gemini-3-flash-preview
GEMINI_MODEL_RECORDER=gemini-3-flash-preview

# Gemini ?��??�數（可?��?
GEMINI_TEMPERATURE=0.7
GEMINI_TOP_P=0.95
GEMINI_TOP_K=40
GEMINI_MAX_OUTPUT_TOKENS=2048
```

### 4. ?��??��?

```bash
cd backend
python main.py
```

### 5. 使用 UI ?��?

1. ?��??�覽?�訪??`http://localhost:8000`
2. ?�右上�??�到?�模?��??�」�???
3. 點�??��??�至 Gemini??
4. 首次?��??��??��?置�?話�?
5. 輸入 API Key ?�模??ID（可?��?
6. 點�??��?證�?置�?
7. 驗�??��?後�??�「�?存並?��???

---

## ?�� Fine-tuning 模�?準�?

### 準�?訓練?��?

系統已自?�收?��?練數?��?位於 `backend/training_data/` ?��???

### 使用 Google AI Studio Fine-tune

1. 訪�? [Google AI Studio](https://aistudio.google.com/)
2. ?��??�Tuned models??
3. 點�??�New tuned model??
4. 上傳訓練?��?（JSONL ?��?�?
5. ?�置訓練?�數
6. ?��?訓練
7. 訓練完�?後獲?�模??ID（格式�?`tunedModels/xxx`�?

### ?�置 Fine-tuned 模�?

??`.env` ?�件中設置�?

```bash
GEMINI_MODEL_SCAMMER=tunedModels/scammer-model-xxxxx
GEMINI_MODEL_VICTIM=tunedModels/victim-model-xxxxx
GEMINI_MODEL_EXPERT=tunedModels/expert-model-xxxxx
GEMINI_MODEL_RECORDER=tunedModels/recorder-model-xxxxx
```

?�在 UI ?�置對話框中輸入??

---

## ?? ?�能??��

### ?��?實�?統�?

```python
from utils.gemini_metrics import gemini_metrics

# ?��?統�??��?
summary = gemini_metrics.get_summary()
print(summary)
```

### 統�??��?

- **請�?統�?**: 總�?求數?��??��??�失?��?
- **Token 使用**: 輸入 Token?�輸??Token?�總�?
- **?��??��?**: 平�??��?小、�?�?
- **?�本估�?**: 美�??�港�?
- **??Agent 統�?**: 每�?Agent ?�詳細數??

### 保�??��?

```python
# 保�??��?認�?�?
gemini_metrics.save_report()

# 保�??��?定�?�?
gemini_metrics.save_report("my_report.json")
```

---

## ?�� ?�本估�?

### Gemini 2.0 Flash 定價�?024�?

- **輸入**: $0.075 / 1M tokens
- **輸出**: $0.30 / 1M tokens

### 估�?示�?

?�設一次�??��?話�?15 輪�?�?
- 輸入: ~15,000 tokens
- 輸出: ~10,000 tokens
- ?�本: $0.075 ? 0.015 + $0.30 ? 0.01 = **$0.00413** (~HK$0.032)

每天 100 次�?話�?
- ?�本: $0.413 (~HK$3.22)

每�? 3,000 次�?話�?
- ?�本: $12.39 (~HK$96.64)

### ?�本?��?建議

1. **使用緩�?**: ?�似?��?使用緩�??��?
2. **Prompt ?��?**: 減�?輸入 Token ?��?
3. **?��??��?**: ?�併多個�?�?
4. **設置?��?**: 每日?�大�?求數?�制
5. **?��?策略**: API ?��??�盡?�自?��??��? Ollama

---

## ?? 安全??

### API Key 管�?

- ??存儲??`.env` ?�件（�??�交??Git�?
- ???��?變�??��?
- ???�端永�??�露 API Key
- ???�??API 調用?��?後端�??

### ?�產?��?建議

1. 使用 Secret Manager（�? AWS Secrets Manager�?
2. 實現 Rate Limiting
3. 添�? API Key 輪�?機制
4. ??��?�常使用模�?

---

## ?�� 測試

### ?��??��?測試

```bash
cd backend
pytest tests/test_gemini_llm.py -v
```

### ?��??��?測試

```bash
pytest tests/test_model_integration.py -v
```

### 測試?�實 API（�?�?API Key�?

```bash
export GEMINI_API_KEY=your_real_api_key
pytest tests/test_gemini_llm.py::TestGeminiLlmIntegration -v
```

---

## ?? ?��??�除

### ?��? 1: API Key ?��?

**?�誤**: `GEMINI_API_KEY ?�設置` ??`API Key ?��??��?`

**�?��?��?**:
1. 檢查 `.env` ?�件中�? `GEMINI_API_KEY`
2. 確�? API Key ?�度 > 10 字�?
3. ??Google AI Studio ?�新?��? API Key

### ?��? 2: 模�?不�???

**?�誤**: `Model not found: tunedModels/xxx`

**�?��?��?**:
1. 確�? fine-tuned 模�?已�?練�???
2. 檢查模�? ID ?�否�?��
3. 使用默�?模�? `gemini-3-flash-preview` 測試

### ?��? 3: API ?��?超�?

**?�誤**: `429 Too Many Requests` ??`Quota exceeded`

**�?��?��?**:
1. 檢查 Google Cloud Console ?��?設置
2. 等�??��??�置（通常每�??��?
3. ?��???Ollama 模�?
4. ?��? API 計�?

### ?��? 4: ?��?超�?

**?�誤**: `Gemini API 請�?超�?`

**�?��?��?**:
1. 增�?超�??��?：`GEMINI_TIMEOUT=120.0`
2. 檢查網絡??��
3. 減�? `MAX_OUTPUT_TOKENS`

### ?��? 5: ?��?後無??

**?�誤**: ?��?後�?使用?�模??

**�?��?��?**:
1. ?�新?�覽?��???
2. 檢查 `.env` ?�件?�否?�新
3. ?��?後端?��?

---

## ?? API ?��?

### 模�??�??API

```http
GET /api/model/status
```

**?��?**:
```json
{
  "success": true,
  "current_provider": "ollama",
  "gemini_enabled": false,
  "gemini_configured": false,
  "provider_info": {
    "provider": "ollama",
    "models": {...}
  }
}
```

### ?��?模�? API

```http
POST /api/model/switch
Content-Type: application/json

{
  "use_gemini": true,
  "gemini_api_key": "optional_api_key",
  "gemini_models": {
    "scammer": "tunedModels/xxx",
    "victim": "tunedModels/yyy"
  }
}
```

### 驗�??�置 API

```http
POST /api/model/validate
Content-Type: application/json

{
  "api_key": "your_api_key",
  "scammer_model": "gemini-3-flash-preview",
  "victim_model": "gemini-3-flash-preview",
  "expert_model": "gemini-3-flash-preview",
  "recorder_model": "gemini-3-flash-preview"
}
```

### 保�??�置 API

```http
POST /api/model/config/gemini
Content-Type: application/json

{
  "api_key": "your_api_key",
  "scammer_model": "gemini-3-flash-preview",
  "victim_model": "gemini-3-flash-preview",
  "expert_model": "gemini-3-flash-preview",
  "recorder_model": "gemini-3-flash-preview"
}
```

---

## ?? ?�佳實�?

### 1. ?�發?��?

- 使用 Ollama（�?費、本?��?
- 快速迭�??測試

### 2. 測試?��?

- 使用 Gemini 默�?模�?
- 驗�? API ?��?
- 測試?�本估�?

### 3. ?�產?��?

- 使用 Fine-tuned 模�?
- ?�用??��?��?�?
- 設置?�本上�?

### 4. 混�?使用

- 高峰?�使??Ollama
- 低峰?�使??Gemini
- ?��??�本?��??��?

---

## ?? �?Ollama ?�移??Gemini

### 步�? 1: 準�??��?

```bash
# 導出 Ollama 訓練?��?
python scripts/export_training_data.py
```

### 步�? 2: Fine-tune 模�?

??Google AI Studio 上傳?��?並�?練�?

### 步�? 3: ?�置模�? ID

?�新 `.env` ?�件??

### 步�? 4: 測試

```bash
# ?��???Gemini
curl -X POST http://localhost:8000/api/model/switch \
  -H "Content-Type: application/json" \
  -d '{"use_gemini": true}'

# 測試對話
curl -X POST http://localhost:8000/api/game/v2/message \
  -H "Content-Type: application/json" \
  -d '{"session_id": "test", "message": "你好"}'
```

### 步�? 5: ??��

```bash
# ?��?統�?
curl http://localhost:8000/api/model/status
```

---

## ?? ?�援

如�??��?，�?�?
1. ?��??��?檔�??��??�除?��?
2. 檢查 `backend/logs/` ?��??�件
3. ?�交 Issue ??GitHub

---

**?�新?��?**: 2026-02-11  
**?�本**: v2.1.0
